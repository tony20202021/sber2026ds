# Plan: Предсказание пола по транзакциям (Sber 2026 DS)

## Цель

Предсказать пол клиента (`0` — женщина, `1` — мужчина) по истории банковских транзакций.

**Целевые метрики:** ROC AUC > 0.88 и Accuracy > 0.80

**Датасет:** [`mks-logic/gender_prediction`](https://huggingface.co/datasets/mks-logic/gender_prediction)
— 3 751 083 транзакций, 8 400 клиентов (70% train / 10% val / 20% test).

---

## Окружение

```bash
conda activate sber   # python 3.11
# зависимости: pandas, numpy, scikit-learn, lightgbm, matplotlib, huggingface_hub
```

```bash
python download_dataset.py        # скачать данные в ./data/
python solution.py                # основное решение (best result)
python feature_importance.py      # анализ важности фич + сравнение cutoff
python solution_linear.py         # LogisticRegression
python solution_random_forest.py  # RandomForest
```

---

## Задачи

- [x] **1. Conda окружение** `sber`, конвертация ноутбука в `solution.py`
- [x] **2. Целевые метрики** (AUC > 0.88, Accuracy > 0.80) — достигнуты в `solution_2026_04_06`
- [x] **3. Feature importance** — график top-50, оптимальный cutoff (top-100)
  - ветка: `solution_2026_04_15_feature_importance`
  - результаты: `results/solution_2026_04_06/`
- [x] **4. Сравнение моделей** — LogReg и RandomForest, метрики + время
  - ветки: `solution_2026_04_15_linear`, `solution_2026_04_15_random_forest`
- [ ] **5. `main.py`** — ветка `solution_2021_old`
  - запустить с минимальными изменениями (близко к оригинальному ноутбуку)
  - сохранить метрики, время, feature importance
  - проанализировать: какие важные фичи есть там, но отсутствуют в `solution_2026_04_06`
- [ ] **6. LightAutoML** — ветка `solution_2026_04_15_lama`
  - найти и установить библиотеку LightAutoML (Sber AI Lab)
  - запустить на текущем датасете
  - сравнить с ручным решением

---

## Результаты (из `results/overall.json`)

| Ветка | AUC | Accuracy | Train | Infer | Фичи |
|---|---|---|---|---|---|
| `solution_2026_04_06` | **0.882 ✓** | **0.802 ✓** | — | — | 376 |
| `solution_2026_04_15_feature_importance` | 0.871 | 0.791 | 12.6s | 0.086s | 100 |
| `solution_2026_04_15_random_forest` | 0.870 | 0.790 | 16.9s | 0.080s | 100 |
| `solution_2026_04_15_linear` | 0.852 | 0.770 | **0.13s** | **0.005s** | 100 |

---

## Feature Engineering (376 признаков, `solution.py`)

| Группа | Признаки |
|---|---|
| Глобальные amount | count, sum, mean, std, min, max, q25, q75, median, cv, range |
| Pos/neg split | count, sum, mean, std отдельно + pos_ratio, pos_neg_ratio |
| Diversity | nunique MCC, nunique tr_type, term_missing_ratio |
| Temporal | hour mean/std, dow ratios (0–6), weekend/morning/afternoon/evening/night ratio, month nunique |
| Active span | active_days, tx_per_day, amount_per_day, avg/std days between tx |
| Recency | recent_ratio, recent_amount_mean, early_amount_mean, amount_trend |
| Entropy | MCC entropy, tr_type entropy |
| Weekend vs weekday | weekend/weekday amount mean, diff |
| Amount buckets | 6 бинов: очень neg / neg / ~0 / small pos / mid pos / large pos |
| Per-MCC (60 кодов) | cnt, mean, std, share |
| Per-tr_type (25 типов) | cnt, mean, share |
| Target encoding (OOF) | mcc_code_te, tr_type_te — p(male) для каждой категории |

**Топ-5 по importance:** `mcc_code_te`, `dow0_ratio`, `dow6_ratio`, `hour_std`, `afternoon_ratio`

---

## Идеи для будущих улучшений

- Sequence features: тренд по категориям, Word2Vec по MCC
- MCC × hour / MCC × weekend — взаимодействия
- CatBoost, XGBoost, Stacking
- Neural network с embedding для MCC/tr_type
- Bayesian hyperparameter search (Optuna)

---

## Структура репозитория

```
sber2026ds/
├── solution.py                  # основное решение (best result)
├── feature_importance.py        # анализ фич, сравнение cutoff
├── solution_linear.py           # LogisticRegression
├── solution_random_forest.py    # RandomForest
├── download_dataset.py          # загрузка датасета
├── requirements.txt
├── .plan.md                     # этот файл
├── data/                        # gitignored
└── results/                     # gitignored
    ├── overall.json
    └── solution_2026_04_06/
        ├── feature_importance.csv
        ├── feature_importance_top50.png
        └── cutoff_curve.png
```
